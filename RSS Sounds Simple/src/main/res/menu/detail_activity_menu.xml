<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:RSS="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".DetailActivity">

    <item android:id="@+id/action_share"
        android:title="@string/share"
        RSS:showAsAction="ifRoom"
        RSS:actionProviderClass="android.support.v7.widget.ShareActionProvider"/>

    <item android:id="@+id/favorites"
        android:title="Save to Favorites"
        RSS:showAsAction="ifRoom"
        android:visible="false"
        android:enabled="false"
        android:icon="@drawable/ic_rating_important"
/>
    <item android:id="@+id/not_favorite"
        android:title="Remove from Favorites"
        RSS:showAsAction="ifRoom"
        android:icon="@drawable/ic_rating_not_important"/>

</menu>